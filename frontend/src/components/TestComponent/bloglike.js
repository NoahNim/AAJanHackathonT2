'use client'

export const BlogLike = () => {

    const handleClick = () => {
        
    }

    return (
        <div>
            <button onClick={handleClick}>Like the Blog</button>
        </div>
    )
}