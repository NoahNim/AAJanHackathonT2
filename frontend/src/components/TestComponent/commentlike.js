'use client'

export const CommentLike = () => {

    const handleClick = () => {
        
    }

    return (
        <div>
            <button onClick={handleClick}>Like the Comment</button>
        </div>
    )
}